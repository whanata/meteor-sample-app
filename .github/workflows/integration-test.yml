name: Integration Tests
on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Cache dependencies
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: npm-${{ hashFiles('package-lock.json') }}
          restore-keys: npm-

      - uses: meteorengineer/setup-meteor@v1
        with:
          meteor-release: '2.1.1'

      - name: Install dependencies
        shell: bash -l {0}
        run: meteor npm ci --ignore-scripts

      - name: Start Meteor App
        shell: bash -l {0}
        run: nohup meteor npm run start &

      - name: Install Wait Port
        shell: bash -l {0}
        run: npm install -g wait-port

      - name: Install Meteor Ping
        shell: bash -l {0}
        run: npm install -g meteor-ping

      - name: Wait Until Meteor App is Up
        shell: bash -l {0}
        run: wait-port 3000 && meteor-ping --connectTimeout 600000 --collection 'meteor.links'

      - name: Run Integration Tests
        shell: bash -l {0}
        run: ./scripts/integration-test.sh
